package at.fhj.ima.kanbancollab.kanbancollab

import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder

fun main() {
    //print(BCryptPasswordEncoder().encode("admin"));
    //print(BCryptPasswordEncoder().encode("user1"));
    //print(BCryptPasswordEncoder().encode("user2"));
    //print(BCryptPasswordEncoder().encode("user3"));
    //print(BCryptPasswordEncoder().encode("user4"));
    //print(BCryptPasswordEncoder().encode("user5"));
    //print(BCryptPasswordEncoder().encode("user"));
}